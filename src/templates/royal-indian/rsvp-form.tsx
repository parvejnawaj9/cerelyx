// Royal Indian reuses the shared, theme-tokenized RSVP form (meal choices,
// custom questions, name prefill) so there's one source of truth.
export { RsvpForm } from "@/templates/shared/rsvp-form";
